# 📋 계획서: 매수 조건 AND 변경 및 10시 이전 돌파 무시 필터

> 작성일: 2026-04-27
> 마지막 업데이트: 2026-04-27
> 현재 단계: ✅ 완료 (2026-04-27)

---

## 🎯 목표

현재 매수 진입 조건을 아래와 같이 변경한다.

1. **AND 조건**: 전통 터틀 트레이딩(20일/55일 신고가 돌파) AND 30분 타임가드를 동시에 만족해야 진입
2. **10시 이전 돌파 무시**: 오전 9시~10시 사이에 발생한 신고가 돌파 신호는 무시. 오전 10시 이후에 발생한 돌파 신호에 한해서만 30분 확인 후 진입

현재 OR 조건(둘 중 하나만 충족해도 진입)을 AND 조건(둘 다 충족해야 진입)으로 바꾸고,
한국 장 특성상 가짜 돌파가 많은 오전 9~10시 구간을 통째로 차단한다.

---

## 📌 전체 진행 현황

| 단계 | 설명 | 상태 |
|------|------|------|
| P0-1 | 계획서 파일 생성 | ✅ |
| P1-1 | 자료 수집 | ✅ |
| P1-2 | 자료 검증 | ✅ |
| P1-3 | 부족한 부분 확인 및 보완 | ✅ |
| P2-1 | 설계안 작성 | ✅ |
| P2-2 | 설계안 검증 | ✅ |
| P2-3 | 프로젝트 구조 작성 | ✅ |
| P3-1 | Phase별 구현 계획 수립 | ✅ |
| P3-2 | Phase A — target_manager.py | ✅ |
| P3-3 | Phase B — timer_agent.py | ✅ |
| P3-4 | Phase C — turtle_order_logic.py | ✅ |
| P4-1 | 단위 테스트 | ✅ |
| P4-2 | 통합 테스트 | ✅ |
| P4-3 | 무한루프·오류 안전성 확인 | ✅ |
| P5-1 | 최종 사용자 승인 | ✅ |
| P5-2 | 관련 문서 업데이트 | ✅ |

---

## PHASE 1 — 자료 수집 및 검증

### P1-1 자료 수집 ⬜

#### 확인 항목
- [ ] timer_agent.py — 진입 신호 생성 전체 흐름
- [ ] target_manager.py — 목표가 및 30분 가드 타이머 관리
- [ ] turtle_order_logic.py — 진입 신호 수신 후 주문 실행 흐름
- [ ] 현재 OR 조건이 정확히 어디서 구현되어 있는지 확인
- [ ] 돌파 시각이 기록/비교되는 곳 확인

#### 수집 결과

**수정이 필요한 파일 4개:**

| 파일 | 수정 이유 |
|------|---------|
| `target_manager.py` | turtle 돌파 발생 시각을 새로 기록해야 함 (현재는 기록 없음) |
| `timer_agent.py` | OR → AND 로직 변경, TARGET_30MIN 경로 제거, 10시 필터 추가 |
| `turtle_order_logic.py` | TARGET_30MIN 관련 레이블/매핑 정리 |
| `test_p4_integration.py` | 테스트 케이스가 구 로직 기반 → 신 로직에 맞게 갱신 필요 |

**현재 코드 구조 파악:**

`timer_agent.py` — 현재 3개 경로(OR):
- 경로 A (TARGET_30MIN): 동적 목표가 위에서 30분 유지 → 즉시 진입
- 경로 B (TURTLE_S2): 55일 신고가 돌파 → **즉시 진입 (30분 가드 없음)**
- 경로 C (TURTLE_S1): 20일 신고가 돌파 → **즉시 진입 (30분 가드 없음)**

`target_manager.py` — 현재 기록 중인 필드:
- `turtle_s1_signal` / `turtle_s2_signal`: 돌파 여부(True/False)만 기록
- **돌파가 언제 발생했는지 시각은 기록하지 않음** ← 핵심 누락 사항

`turtle_order_logic.py`:
- `entry_source` 기본값이 `"TARGET_30MIN"` → 변경 필요
- TARGET_30MIN 레이블 매핑 2군데 → 정리 필요

---

### P1-2 자료 검증 ✅

#### 검증 결과

**수정 위치 정밀 확인:**

| 파일 | 위치 | 수정 내용 |
|------|------|---------|
| `target_manager.py` | 342~380행 | 코드 첫 등록/업데이트 블록에 breakout_since 로직 삽입 |
| `timer_agent.py` | 183~240행 | run_timer_check() 내 3경로 → 2경로(AND) 변경 |
| `turtle_order_logic.py` | 250, 322~326, 343~346행 | TARGET_30MIN 기본값 및 레이블 제거 |
| `test_p4_integration.py` | 129~224행 | 기존 테스트 7개 → 신규 로직에 맞게 재작성 |

**검증 결론:**
- 의존 관계 이상 없음 (4개 파일 외에 다른 파일에 영향 없음)
- `above_target_since`, `pending_target` 필드는 target_manager.py 로그에서만 쓰이고
  진입 결정에는 더 이상 관여하지 않으므로, 그대로 유지해도 문제 없음 (로그 참고용으로 남김)
- TARGET_30MIN이 사라져도 체결 원장(trade_ledger)에는 ENTRY_S1 / ENTRY_S2만 기록되어 기존 구글 시트와 호환됨

---

### P1-3 부족한 부분 확인 및 보완 ✅

#### 보완 결과

**확정된 진입 규칙 (사용자 확인 완료):**

돌파 시각이 언제이든 `turtle_s1_breakout_since`를 기록한다.
진입 조건은 아래 두 가지를 **동시에** 충족할 때:

1. 돌파 시각 기준으로 30분 이상 경과
2. 현재 시각이 오전 10시 이상

| 돌파 시각 | 30분 도달 시각 | 실제 진입 시각 |
|-----------|--------------|-------------|
| 9시 20분 | 9시 50분 (10시 이전) | **10시 00분** (10시 최소 보장) |
| 9시 40분 | 10시 10분 | **10시 10분** |
| 10시 30분 | 11시 00분 | **11시 00분** |
| 14시 20분 | 14시 50분 | **14시 50분** |

**구현 변경점 수정:**
- `target_manager.py`: 돌파 시각 기록 시 10시 체크 불필요 → 항상 기록
- `timer_agent.py`: 30분 경과 AND 현재시각 >= 10:00 두 조건 모두 확인

---

## PHASE 2 — 설계

### P2-1 설계안 작성 ✅

#### 설계안

---

##### 핵심 변경 요약

**Before (OR 방식):**
```
진입 신호 = 목표가 30분 상회  OR  20일 신고가 돌파  OR  55일 신고가 돌파
            (TARGET_30MIN)        (TURTLE_S1)           (TURTLE_S2)
            → 즉시 진입                → 즉시 진입           → 즉시 진입
```

**After (AND 방식):**
```
진입 신호 = (20일 신고가 돌파 OR 55일 신고가 돌파)
            AND 돌파 시각으로부터 30분 이상 경과
            AND 현재 시각 >= 오전 10:00
```

---

##### 파일별 설계

**[파일 1] target_manager.py — 돌파 시각 기록 추가**

새 필드 2개를 `unheld_stock_record.json`에 추가:
- `turtle_s1_breakout_since`: S1(20일) 신호가 처음 True가 된 시각 (None이면 신호 없음)
- `turtle_s2_breakout_since`: S2(55일) 신호가 처음 True가 된 시각

관리 규칙:
```
turtle_s1_signal이 False → True 전환  : breakout_since = 지금 시각 기록
turtle_s1_signal이 True  → True 유지  : breakout_since 그대로 보존 (변경 안 함)
turtle_s1_signal이 True  → False 전환 : breakout_since = None 초기화
```
→ S2도 동일 규칙 적용

수정 위치:
1. `initialize_unheld_record()` — 신규 종목 초기화 시 두 필드를 None으로 추가
2. `run_update()` — 코드 첫 등록 블록(342행)과 기존 업데이트 블록(374행) 두 곳에 로직 삽입

---

**[파일 2] timer_agent.py — OR → AND 변경**

기존 `check_30min_passed()` 함수: TARGET_30MIN용 → **역할 변경 또는 내부 헬퍼로 대체**

새 내부 헬퍼 함수 `_check_turtle_30min(code, breakout_since_key, unheld_record)`:
```
① unheld_record[code][breakout_since_key] 값이 있는지 확인
② 현재 시각 >= 10:00인지 확인 (아니면 False 반환, 대기 메시지 출력)
③ 돌파 시각부터 장중 경과 분 계산 (기존 _market_minutes_elapsed() 재사용)
④ 30분 이상이면 True, 미달이면 False (남은 분 출력)
```

`run_timer_check()` 변경:
```
경로 A (TARGET_30MIN) — 완전 제거
경로 B (TURTLE_S2)    — turtle_s2_signal True AND _check_turtle_30min() 통과 시 신호
경로 C (TURTLE_S1)    — turtle_s1_signal True AND _check_turtle_30min() 통과 시 신호
우선순위: TURTLE_S2 > TURTLE_S1 (기존 유지)
```

정렬 로직 단순화: TARGET_30MIN 제거 → TURTLE_S2(0) / TURTLE_S1(1)만 남음

---

**[파일 3] turtle_order_logic.py — TARGET_30MIN 흔적 제거**

| 위치 | 변경 전 | 변경 후 |
|------|--------|--------|
| 250행 기본값 | `entry_source: str = "TARGET_30MIN"` | `entry_source: str = "TURTLE_S1"` |
| 322행 source_map | `"TARGET_30MIN": "ENTRY_30MIN"` 항목 제거 | — |
| 326행 fallback | `source_map.get(entry_source, "ENTRY_30MIN")` | `source_map.get(entry_source, "ENTRY_S1")` |
| 345행 label | `"TARGET_30MIN": "목표가30분"` 항목 제거 | — |
| 주석/docstring | TARGET_30MIN 언급 제거 | — |

---

**[파일 4] test_p4_integration.py — 테스트 케이스 갱신**

제거:
- `test_경로A_TARGET_30MIN` — TARGET_30MIN 경로 없어짐
- `test_경로A_30분_미달_신호_없음` — 동일
- `test_중복_S2가_30MIN보다_우선` — TARGET_30MIN 없어짐
- `test_중복_S1이_30MIN보다_우선` — 동일

변경:
- `test_경로B_TURTLE_S1_30분_가드_없음` → `test_경로B_TURTLE_S1_30분_가드_필요`
  (즉시 신호 → 30분+10시 조건 있어야 신호)
- `test_경로B_TURTLE_S2` → 동일하게 30분 가드 필요로 수정

추가:
- `test_S1_10시이전_진입불가` — 30분 지났어도 10시 전이면 신호 없음
- `test_S1_9시40분돌파_10시10분진입` — 9:40 돌파, 10:00엔 아직 20분 → 10:10에 진입
- `test_S1_9시20분돌파_10시바로진입` — 9:20 돌파, 10:00엔 이미 40분 → 10:00 즉시 진입

---

##### 데이터 흐름 요약

```
target_manager.run_update()
  ↓ turtle 신호 계산
  ↓ turtle_s1_breakout_since 기록 (신호 True 전환 시)
  ↓ unheld_stock_record.json 저장

timer_agent.run_timer_check()
  ↓ turtle_s1_signal == True ?
  ↓ AND _check_turtle_30min() → 30분 경과 AND 10시 이후 ?
  ↓ 통과 시 entry_signals에 추가

turtle_order_logic.run_orders(entry_signals)
  ↓ 주문 실행 (entry_source: TURTLE_S1 / TURTLE_S2)
```

---

### P2-2 설계안 검증 ✅

#### 검증 결과

**시나리오별 동작 시뮬레이션:**

| 시나리오 | 돌파 시각 | 10:00 경과분 | 진입 시각 | 판정 |
|---------|---------|------------|---------|-----|
| 9:20 돌파, 유지 | 9:20 | 40분 | **10:00 즉시** | ✅ 올바름 |
| 9:40 돌파, 유지 | 9:40 | 20분 | **10:10** | ✅ 올바름 |
| 9:55 돌파, 유지 | 9:55 | 5분 | **10:25** | ✅ 올바름 |
| 10:20 돌파 | 10:20 | — | **10:50** | ✅ 올바름 |
| 9:30 돌파 → 9:50 이탈 → 10:15 재돌파 | 10:15 | — | **10:45** | ✅ 올바름 |
| 9:30 돌파, 10시 전 확인 | 9:30 | — | 없음(10시 전) | ✅ 올바름 |

**`_market_minutes_elapsed()` 재사용 검증:**
- 9:20 시작, 10:00 체크: `max(9:20, 9:00) ~ min(10:00, 15:30)` = 40분 ✅
- 9:40 시작, 10:05 체크: 25분 → 아직 30분 미달 ✅
- 9:40 시작, 10:10 체크: 30분 → 통과 ✅

**기존 함수 처리:**
- `check_30min_passed()` — TARGET_30MIN 전용이었으므로 **삭제**, 새 `_check_turtle_30min()`으로 대체
- `_market_minutes_elapsed()` — 그대로 재사용 (변경 없음)

**위험 요소:**
- 구 `unheld_stock_record.json`(새 필드 없음)으로 시작하면?
  → `setdefault`로 None 초기화 후, turtle_s1이 True면 지금 시각으로 즉시 기록 → 안전 ✅
- run_update()가 여러 번 호출되면 breakout_since가 덮어씌워지나?
  → "None인 경우에만 기록" 조건으로 방지 ✅
- `check_30min_passed()`를 외부에서 import해서 쓰는 곳이 있나?
  → 전체 코드 검색 결과 timer_agent.py 내부에서만 사용 → 삭제 안전 ✅

---

### P2-3 프로젝트(코드) 구조 작성 ✅

#### 수정 파일 목록 (신규 파일 없음, 기존 4개 수정)

```
target_manager.py    ← 돌파 시각 필드 추가 (3곳)
timer_agent.py       ← OR→AND 변경, 10시 필터 추가 (전면 재작성)
turtle_order_logic.py ← TARGET_30MIN 흔적 제거 (3곳)
test_p4_integration.py ← 테스트 케이스 갱신
```

---

#### [target_manager.py] 수정 위치 3곳

**수정 1 — initialize_unheld_record() 132~138행: 새 필드 2개 추가**
```python
# 변경 전
unheld_record[code] = {
    "pending_target":     init_target,
    "reference_price":    current_price,
    "above_target_since": None,
    "turtle_s1_signal":   False,
    "turtle_s2_signal":   False,
    "last_updated":       now_str,
}

# 변경 후
unheld_record[code] = {
    "pending_target":           init_target,
    "reference_price":          current_price,
    "above_target_since":       None,
    "turtle_s1_signal":         False,
    "turtle_s2_signal":         False,
    "turtle_s1_breakout_since": None,   # ← 추가: S1 돌파 발생 시각
    "turtle_s2_breakout_since": None,   # ← 추가: S2 돌파 발생 시각
    "last_updated":             now_str,
}
```

**수정 2 — run_update() 342~352행 (처음 등록): 새 필드 + 즉시 기록**
```python
# 변경 전
unheld_record[code] = {
    "pending_target":     new_target,
    "reference_price":    current_price,
    "above_target_since": None,
    "turtle_s1_signal":   turtle_s1,
    "turtle_s2_signal":   turtle_s2,
    "last_updated":       None,
}

# 변경 후
now_kst_str = datetime.now(KST).strftime("%Y-%m-%d %H:%M:%S")
unheld_record[code] = {
    "pending_target":           new_target,
    "reference_price":          current_price,
    "above_target_since":       None,
    "turtle_s1_signal":         turtle_s1,
    "turtle_s2_signal":         turtle_s2,
    "turtle_s1_breakout_since": now_kst_str if turtle_s1 else None,
    "turtle_s2_breakout_since": now_kst_str if turtle_s2 else None,
    "last_updated":             None,
}
```

**수정 3 — run_update() 374~380행 (기존 업데이트): 전환 감지 로직 추가**
```python
# 변경 전
unheld_record[code]["turtle_s1_signal"] = turtle_s1
unheld_record[code]["turtle_s2_signal"] = turtle_s2
unheld_record[code].setdefault("turtle_s1_signal", turtle_s1)
unheld_record[code].setdefault("turtle_s2_signal", turtle_s2)

# 변경 후
now_kst_str = datetime.now(KST).strftime("%Y-%m-%d %H:%M:%S")

# 구버전 호환: 새 필드 없는 JSON 대비
unheld_record[code].setdefault("turtle_s1_breakout_since", None)
unheld_record[code].setdefault("turtle_s2_breakout_since", None)

# S1 신호 업데이트 + 돌파 시각 관리
prev_s1 = unheld_record[code].get("turtle_s1_signal", False)
unheld_record[code]["turtle_s1_signal"] = turtle_s1
if turtle_s1:
    # 신호 True인데 시각 미기록이면 지금 시각으로 기록 (전환 포함)
    if unheld_record[code]["turtle_s1_breakout_since"] is None:
        unheld_record[code]["turtle_s1_breakout_since"] = now_kst_str
else:
    # 신호 사라지면 시각 초기화
    unheld_record[code]["turtle_s1_breakout_since"] = None

# S2 신호 업데이트 + 돌파 시각 관리 (동일 구조)
unheld_record[code]["turtle_s2_signal"] = turtle_s2
if turtle_s2:
    if unheld_record[code]["turtle_s2_breakout_since"] is None:
        unheld_record[code]["turtle_s2_breakout_since"] = now_kst_str
else:
    unheld_record[code]["turtle_s2_breakout_since"] = None
```

---

#### [timer_agent.py] 전면 수정

**제거:** `check_30min_passed()` 함수 전체 (85~138행)

**추가:** `_check_turtle_30min()` 헬퍼 함수 (같은 위치에 대체)
```python
def _check_turtle_30min(code: str, breakout_since_key: str, unheld_record: dict) -> bool:
    """터틀 돌파 시각으로부터 30분 경과 + 오전 10시 이후인지 확인한다."""
    stock_data = unheld_record.get(code)
    if not stock_data:
        return False
    since_str = stock_data.get(breakout_since_key)
    if not since_str:
        return False
    try:
        since   = KST.localize(datetime.strptime(since_str, "%Y-%m-%d %H:%M:%S"))
        now_kst = datetime.now(KST)
        name    = get_watchlist().get(code, {}).get("name", code)

        # 조건 1: 오전 10시 이상이어야 진입 가능
        if now_kst.hour < 10:
            print(f"[timer_agent] {name}({code}) ⏳ 10시 이전 진입 불가 "
                  f"(현재 {now_kst.strftime('%H:%M')})")
            return False

        # 조건 2: 돌파 시각으로부터 장중 30분 이상 경과
        elapsed = _market_minutes_elapsed(since, now_kst)
        if elapsed >= GUARD_MINUTES:
            print(f"[timer_agent] {name}({code}) ✅ 터틀 30분 가드 통과! "
                  f"(장중 {int(elapsed)}분 경과, 돌파 시각: {since_str})")
            return True
        else:
            remaining = int(GUARD_MINUTES - elapsed) + 1
            print(f"[timer_agent] {name}({code}) ⏳ 대기 중 "
                  f"(앞으로 약 {remaining}분 더 필요, 돌파 시각: {since_str})")
            return False
    except (ValueError, TypeError) as e:
        print(f"[timer_agent] {code} 타이머 시각 파싱 오류: {e}")
        return False
```

**변경:** `run_timer_check()` 내 경로 처리 (171~222행)
```python
# 변경 전 우선순위 주석
# 우선순위: TURTLE_S2(0) > TURTLE_S1(1) > TARGET_30MIN(2)

# 변경 후 우선순위 주석
# 우선순위: TURTLE_S2(0) > TURTLE_S1(1)
# 진입 조건: 신고가 돌파 AND 돌파 시각부터 30분 경과 AND 오전 10시 이후

# 경로 A (TARGET_30MIN) — 완전 제거

# 경로 C: 터틀 시스템1 — 20일 신고가 + 30분 가드 (우선순위 1)
if data.get("turtle_s1_signal", False) and 1 < current_priority:
    if _check_turtle_30min(code, "turtle_s1_breakout_since", unheld_record):
        name = watchlist.get(code, {}).get("name", code)
        print(f"[timer_agent] {name}({code}) ✅ 터틀 S1 진입 신호 (20일 신고가 + 30분 가드)")
        signal_priority[code] = (1, "TURTLE_S1")
        current_priority = 1

# 경로 B: 터틀 시스템2 — 55일 신고가 + 30분 가드 (우선순위 0)
if data.get("turtle_s2_signal", False) and 0 < current_priority:
    if _check_turtle_30min(code, "turtle_s2_breakout_since", unheld_record):
        name = watchlist.get(code, {}).get("name", code)
        print(f"[timer_agent] {name}({code}) ✅ 터틀 S2 진입 신호 (55일 신고가 + 30분 가드)")
        signal_priority[code] = (0, "TURTLE_S2")
```

**변경:** `_sort_key()` 단순화 (TARGET_30MIN 제거)
```python
def _sort_key(s: dict):
    return (0, "") if s["entry_source"] == "TURTLE_S2" else (1, "")
```

**변경:** 결과 출력 부분 (TARGET_30MIN 분기 제거)
```python
# 변경 전
if src == "TARGET_30MIN":
    since = unheld_record[code].get("above_target_since", "?")
    print(f"[timer_agent]   → {name}({code}) [{src}] 안착 시각: {since}")
else:
    print(f"[timer_agent]   → {name}({code}) [{src}]")

# 변경 후
since = unheld_record[code].get(
    "turtle_s2_breakout_since" if src == "TURTLE_S2" else "turtle_s1_breakout_since", "?")
print(f"[timer_agent]   → {name}({code}) [{src}] 돌파 시각: {since}")
```

---

#### [turtle_order_logic.py] 수정 위치 3곳

**수정 1 — 250행 기본값**
```python
# 변경 전
entry_source: str = "TARGET_30MIN"
# 변경 후
entry_source: str = "TURTLE_S1"
```

**수정 2 — 322~326행 source_map**
```python
# 변경 전
source_map = {
    "TARGET_30MIN": "ENTRY_30MIN",
    "TURTLE_S1":    "ENTRY_S1",
    "TURTLE_S2":    "ENTRY_S2",
}
ledger_source = source_map.get(entry_source, "ENTRY_30MIN")

# 변경 후
source_map = {
    "TURTLE_S1": "ENTRY_S1",
    "TURTLE_S2": "ENTRY_S2",
}
ledger_source = source_map.get(entry_source, "ENTRY_S1")
```

**수정 3 — 343~346행 label dict**
```python
# 변경 전
source_label = {
    "TURTLE_S2":    "터틀S2(55일신고가)",
    "TURTLE_S1":    "터틀S1(20일신고가)",
    "TARGET_30MIN": "목표가30분",
}.get(entry_source, entry_source)

# 변경 후
source_label = {
    "TURTLE_S2": "터틀S2(55일신고가)",
    "TURTLE_S1": "터틀S1(20일신고가)",
}.get(entry_source, entry_source)
```

---

#### [test_p4_integration.py] 테스트 케이스 갱신

제거 (4개): test_경로A_TARGET_30MIN, test_경로A_30분_미달_신호_없음,
            test_중복_S2가_30MIN보다_우선, test_중복_S1이_30MIN보다_우선

변경 (2개): test_경로B_TURTLE_S1_30분_가드_없음 → turtle_s1_breakout_since 필드 추가,
            30분 가드 조건 반영

추가 (3개):
- test_S1_10시이전_진입불가: 30분 지났어도 9시대면 신호 없음
- test_S1_9시40분돌파_10시10분에_진입: 20분 경과 → 아직 대기
- test_S1_9시20분돌파_10시에_즉시_진입: 40분 경과 → 바로 신호

---

## PHASE 3 — 구현

### P3-1 Phase별 구현 계획 수립 ✅

| 구현 Phase | 파일 | 작업 내용 |
|-----------|------|---------|
| Phase A | `target_manager.py` | 돌파 시각 필드 추가 및 기록 로직 |
| Phase B | `timer_agent.py` | OR→AND 변경, 10시 필터, 30분 가드 |
| Phase C | `turtle_order_logic.py` | TARGET_30MIN 흔적 제거 |
| Phase D | `test_p4_integration.py` | 테스트 케이스 갱신 |

**순서 근거:**
- Phase A 먼저: timer_agent가 읽을 `turtle_s1_breakout_since` 필드가 먼저 기록돼야 함
- Phase B 다음: A가 만든 데이터를 읽어서 진입 신호 판단
- Phase C 마지막(코드): A·B와 독립적이라 순서 무관하지만 정리 목적으로 마지막
- Phase D: 구현 완료 후 테스트

---

### P3-2 Phase A 구현 — target_manager.py ⬜

#### 작업 목록
- [ ] initialize_unheld_record() — 새 필드 2개 추가
- [ ] run_update() 처음 등록 블록 — 새 필드 + 즉시 기록
- [ ] run_update() 기존 업데이트 블록 — 전환 감지 + breakout_since 관리

#### 구현 결과
(진행 후 채워짐)

---

### P3-3 Phase B 구현 — timer_agent.py ⬜

#### 작업 목록
- [ ] check_30min_passed() 삭제
- [ ] _check_turtle_30min() 새 함수 추가
- [ ] run_timer_check() 경로 A 제거, B·C AND 조건으로 변경
- [ ] _sort_key() 단순화
- [ ] 결과 출력 부분 TARGET_30MIN 분기 제거
- [ ] 모듈 상단 주석 업데이트

#### 구현 결과
(진행 후 채워짐)

---

### P3-4 Phase C 구현 — turtle_order_logic.py ⬜

#### 작업 목록
- [ ] 250행 기본값 변경
- [ ] source_map TARGET_30MIN 제거
- [ ] label dict TARGET_30MIN 제거

#### 구현 결과
(진행 후 채워짐)

---

## PHASE 4 — 테스트

### P4-1 단위 테스트 ⬜
#### 테스트 결과
(진행 후 채워짐)

### P4-2 통합 테스트 ⬜
#### 테스트 결과
(진행 후 채워짐)

### P4-3 무한루프·오류 안전성 확인 ⬜
#### 확인 결과
(진행 후 채워짐)

---

## PHASE 5 — 마무리

### P5-1 최종 사용자 승인 ⬜
### P5-2 관련 문서 업데이트 ⬜

---

## 📝 메모 및 특이사항

(작업 중 기억해야 할 것들을 여기에 기록)

---

## ✅ 최종 완료 체크리스트

- [ ] 모든 Phase 완료 (P0 ~ P5)
- [ ] 단위 테스트 통과
- [ ] 통합 테스트 통과
- [ ] 무한루프 안전성 확인
- [ ] 사용자 최종 승인 완료
- [ ] 관련 문서 업데이트 완료
