# 📋 계획서: Unit 매수 금액 상한 및 종목별 리스크 팩터

> 작성일: 2026-04-27
> 마지막 업데이트: 2026-04-27
> 현재 단계: ✅ 완료 (2026-04-27)

---

## 🎯 목표

1 Unit당 매수 최대 금액을 총 자본금의 10%로 제한하고,
초과 시 해당 종목의 리스크 팩터(위험 비율)를 줄여서 매수금을 한도에 맞게 조정한다.
종목마다 다른 리스크 팩터를 계산해서 저장한다.

---

## 📌 전체 진행 현황

| 단계 | 설명 | 상태 |
|------|------|------|
| P0-1 | 계획서 파일 생성 | ✅ |
| P1-1 | 자료 수집 | ✅ |
| P1-2 | 자료 검증 | ✅ |
| P1-3 | 부족한 부분 확인 및 보완 | ✅ |
| P2-1 | 설계안 작성 | ✅ |
| P2-2 | 설계안 검증 | ✅ |
| P2-3 | 프로젝트 구조 작성 | ✅ |
| P3-1 | Phase별 구현 계획 수립 | ✅ |
| P3-2~ | 단계별 구현 | ✅ |
| P4-1 | 단위 테스트 | ✅ |
| P4-2 | 통합 테스트 | ✅ |
| P4-3 | 무한루프·오류 안전성 확인 | ✅ |
| P5-1 | 최종 사용자 승인 | ✅ |
| P5-2 | 관련 문서 업데이트 | ✅ |

---

## PHASE 1 — 자료 수집 및 검증

### P1-1 자료 수집 ✅

#### 확인 항목
- [x] turtle_order_logic.py — calc_unit_size() 전체 흐름
- [x] held_stock_record.json — 현재 저장 필드 목록
- [x] risk_guardian.py — 2N 손절가 계산 및 저장 방식
- [x] 현재 리스크 팩터(0.02)가 어디서 사용되는지 전체 확인

#### 수집 결과

**수정이 필요한 파일: turtle_order_logic.py 1개**

| 위치 | 현재 코드 | 변경 필요 내용 |
|------|---------|-------------|
| 177행 | `qty = int((total_capital * 0.02) / atr_n)` | 리스크 팩터 0.02를 종목별로 조정 가능하게 변경 |
| 184행 | `max_unit_amount = total_capital / MAX_TOTAL_UNITS` | `total_capital * 0.1` 로 변경 |
| 189~192행 | qty 수량만 클리핑 (리스크 팩터는 그대로 0.02) | 리스크 팩터를 줄여서 qty 재계산하는 방식으로 변경 |
| 299~310행 | position_state에 effective_risk_factor 없음 | `effective_risk_factor` 필드 추가 저장 |
| 598~602행 (피라미딩) | `calc_unit_size()` 재호출 (매번 0.02 기준) | 저장된 `effective_risk_factor` 사용 |

**현재 수량 계산 공식:**
```
qty = int((총자본 × 0.02) / ATR)
매수금 = qty × 현재가
최대금액 = 총자본 / 12  ← 현재
```

**변경 후 공식:**
```
최대금액 = 총자본 × 0.1  ← 변경
매수금 = (총자본 × 0.02 / ATR) × 현재가

매수금 ≤ 최대금액 → effective_risk_factor = 0.02 (변경 없음)
매수금 > 최대금액 → effective_risk_factor = (최대금액 × ATR) / (총자본 × 현재가)
                   qty = int((총자본 × effective_risk_factor) / ATR)
```

**held_stock_record.json 현재 저장 필드:**
```json
{
  "stock_name": ...,
  "current_unit": ...,
  "last_buy_price": ...,
  "avg_buy_price": ...,
  "stop_loss_price": ...,      ← 2N 손절가 (진입/피라미딩마다 갱신)
  "next_pyramid_price": ...,
  "entry_type": ...,
  "max_unit": ...,
  "total_qty": ...,
  "entry_source": ...
}
```
→ 여기에 `"effective_risk_factor"` 필드 추가 필요

---

### P1-2 자료 검증 ✅

#### 검증 결과

**calc_unit_size() 호출 위치 — turtle_order_logic.py 2곳만 사용, 다른 파일 없음:**
| 위치 | 사용 방식 |
|------|---------|
| 534행 (신규 진입) | `qty, entry_type, max_unit = result` → 4번째 값(effective_rf) 언패킹 추가 필요 |
| 598~602행 (피라미딩) | `qty = result[0]` → calc_unit_size 재호출 대신 저장된 effective_risk_factor 사용으로 변경 |

**예외 진입(EXCEPTION) 경로와 충돌 없음:**
- 예외 진입 조건: 1주 가격 > 총자본 × 2%
- 1주 가격이 2%~5% → 1주, max_unit=2 반환
- 이 경우 매수금(1주)은 최대 5%로 새 상한(10%)보다 낮음 → 상한 초과 불가
- → 예외 진입은 effective_risk_factor 계산 대상 아님 → None으로 저장

**lines 186~188 (Unit 상한 초과 스킵) 사실상 dead code:**
- 정상 경로(≤2% 종목)에서 max_unit_amount = 총자본 × 0.1 이므로 max_qty ≥ 5 → 0이 될 수 없음
- 안전 가드로 남겨두되, 실제 발동은 안 됨

**피라미딩 qty 변경 방식 확정:**
```python
# 변경 전: calc_unit_size() 재호출 (매번 0.02 기준)
result = calc_unit_size(code, current_price, atr_n, total_capital)
qty = result[0]

# 변경 후: 저장된 effective_risk_factor 사용
effective_rf = pos.get("effective_risk_factor")
if effective_rf is None:  # EXCEPTION 진입 종목
    qty = 1
else:
    qty = int((total_capital * effective_rf) / atr_n)
    if qty <= 0:
        continue
```

---

### P1-3 부족한 부분 확인 및 보완 ✅

#### 보완 결과

**추가 확인 사항 2가지:**

1. **balance_sync.py 수동 편입**: `held_stock_record.json`에 수동 편입 시 `effective_risk_factor` 필드 없음
   → `entry_type = "MANUAL"`, `max_unit = 1` (피라미딩 불가)이므로 `effective_risk_factor = None` 추가만 하면 됨

2. **테스트 파일**: `calc_unit_size`, `max_unit_amount`, `0.02` 관련 테스트 없음
   → 기존 테스트 파일 수정 불필요, 새 테스트만 추가하면 됨

**최종 수정 파일 확정:**
| 파일 | 수정 내용 |
|------|---------|
| `turtle_order_logic.py` | calc_unit_size() 로직 + 신규진입 저장 + 피라미딩 계산 변경 |
| `balance_sync.py` | 수동 편입 시 `effective_risk_factor = None` 필드 추가 |

---

## PHASE 2 — 설계

### P2-1 설계안 작성 ✅

#### 설계안

---

##### 핵심 변경 요약

**Before:**
```
max_unit_amount = 총자본 / 12
qty = int((총자본 × 0.02) / ATR)
if qty × 현재가 > max_unit_amount:
    qty = int(max_unit_amount / 현재가)  ← 수량만 자름, 리스크팩터는 그대로 0.02
```

**After:**
```
MAX_UNIT_PURCHASE_RATIO = 0.1
max_unit_amount = 총자본 × 0.1

qty_raw = int((총자본 × 0.02) / ATR)
purchase_raw = qty_raw × 현재가

if purchase_raw ≤ max_unit_amount:
    effective_risk_factor = 0.02          ← 그대로 사용
    qty = qty_raw
else:
    effective_risk_factor = (max_unit_amount × ATR) / (총자본 × 현재가)  ← 줄임
    qty = int((총자본 × effective_risk_factor) / ATR)
    # = int(max_unit_amount / 현재가) 와 같은 결과
```

---

##### 파일 1: turtle_order_logic.py

**수정 1 — 상수 추가 (119행 근처)**
```python
# 변경 전
MAX_TOTAL_UNITS = 12

# 변경 후
MAX_TOTAL_UNITS = 12
MAX_UNIT_PURCHASE_RATIO = 0.1  # 1 Unit당 최대 매수금액 = 총자본 × 10%
```

**수정 2 — calc_unit_size() 반환값 변경 (130~200행)**

반환 타입: `(qty, entry_type, max_unit)` → `(qty, entry_type, max_unit, effective_risk_factor)`

```python
# 예외 진입 경로 반환값
return (1, "EXCEPTION", 2, None)   # effective_risk_factor = None (1주 고정)

# 일반 진입 — 기존 qty 계산 후:
max_unit_amount = total_capital * MAX_UNIT_PURCHASE_RATIO
purchase_amount = qty * price

if purchase_amount <= max_unit_amount:
    effective_risk_factor = 0.02
else:
    # 최대 매수금에 맞게 리스크팩터를 줄임
    effective_risk_factor = (max_unit_amount * atr_n) / (total_capital * price)
    qty = int((total_capital * effective_risk_factor) / atr_n)
    if qty <= 0:
        print(f"[turtle] {name}({code}) 유효 수량=0 → 스킵")
        return None
    print(f"[turtle] {name}({code}) 매수금 상한 초과 → "
          f"리스크팩터 0.02 → {effective_risk_factor:.4f}로 조정 "
          f"({qty}주, 매수금: {qty*price:,.0f}원)")

return (qty, "NORMAL", 3, effective_risk_factor)
```

**수정 3 — place_entry_order() 시그니처 및 저장 필드 (241~310행)**
```python
# 변경 전
def place_entry_order(
    code, qty, price, atr_n, entry_type, max_unit, entry_source="TURTLE_S1"
):

# 변경 후
def place_entry_order(
    code, qty, price, atr_n, entry_type, max_unit,
    entry_source="TURTLE_S1", effective_risk_factor=None
):

# position_state 저장 시 필드 추가
position_state[code] = {
    ...기존 필드...,
    "effective_risk_factor": effective_risk_factor,  # ← 추가
}
```

**수정 4 — run_orders() 신규 진입 언패킹 (538행)**
```python
# 변경 전
qty, entry_type, max_unit = result

# 변경 후
qty, entry_type, max_unit, effective_rf = result

# place_entry_order 호출 시 전달
place_entry_order(code, qty, current_price, atr_n,
                  entry_type, max_unit, entry_source, effective_rf)
```

**수정 5 — run_orders() 피라미딩 qty 계산 (597~602행)**
```python
# 변경 전
result = calc_unit_size(code, current_price, atr_n, total_capital)
if result is None:
    continue
qty = result[0]

# 변경 후
effective_rf = pos.get("effective_risk_factor")
if effective_rf is None:
    # EXCEPTION 또는 MANUAL 진입 → 1주 고정
    qty = 1
else:
    qty = int((total_capital * effective_rf) / atr_n)
    if qty <= 0:
        continue
```

---

##### 파일 2: balance_sync.py

**수정 1 — 수동 편입 dict에 필드 추가 (158행)**
```python
held[code] = {
    ...기존 필드...,
    "effective_risk_factor": None,  # ← 추가 (수동 편입은 피라미딩 없음)
}
```

---

##### 데이터 흐름 요약

```
calc_unit_size()
  ↓ effective_risk_factor 계산 (0.02 또는 줄어든 값)
  ↓ (qty, entry_type, max_unit, effective_rf) 반환

run_orders() — 신규 진입
  ↓ effective_rf 언패킹
  ↓ place_entry_order(... effective_rf) 전달

place_entry_order()
  ↓ held_stock_record.json에 effective_risk_factor 저장

run_orders() — 피라미딩
  ↓ pos["effective_risk_factor"] 읽기
  ↓ qty = int((총자본 × effective_rf) / atr_n)
  ↓ place_pyramid_order(code, qty, ...)
```

---

### P2-2 설계안 검증 ✅

#### 검증 결과

| 검증 항목 | 결과 |
|---------|------|
| 상한 초과 시 수식 정확성 | ✅ effective_rf = (max_amount × ATR) / (자본 × 가격) → qty = max_amount / 가격 |
| 상한 이내 시 기존 동작 유지 | ✅ effective_rf = 0.02 그대로 |
| 피라미딩에 effective_rf 적용 | ✅ ATR 변동 시 자연스럽게 수량 반영됨 |
| 예외 진입(EXCEPTION) 충돌 없음 | ✅ price 2~5% → 매수금 ≤ 5% < 상한 10% → 상한 로직 무관 |
| qty = 0 케이스 불가 확인 | ✅ price ≤ 2%, max = 10% → 최소 qty ≥ 5 |
| MANUAL 편입 피라미딩 안전 | ✅ max_unit=1=current_unit → 피라미딩 차단, effective_rf 사용 안 됨 |
| 기존 186~188행 dead code 확인 | ✅ 삭제 대상 (절대 발동 안 됨) |

---

### P2-3 프로젝트(코드) 구조 작성 ✅

#### 수정 파일: 2개 (신규 파일 없음)

---

#### [turtle_order_logic.py] 수정 4곳

**수정 1 — 상수 추가 (123행 뒤)**
```python
# 변경 전
MAX_SECTOR_UNITS = 6

# 변경 후
MAX_SECTOR_UNITS = 6
MAX_UNIT_PURCHASE_RATIO = 0.1  # 1 Unit당 최대 매수금액 = 총자본 × 10%
```

---

**수정 2 — calc_unit_size() 전체 교체 (130~194행)**
```python
def calc_unit_size(code: str, price: int, atr_n: float, total_capital: int):
    """리스크 기반 Unit 수량을 계산한다.

    터틀 트레이딩의 핵심 공식:
    1 Unit 수량 = (총 자본 × 리스크팩터) / ATR(N)

    1 Unit 매수금이 총자본 × 10% 이하이면 리스크팩터 = 0.02 (기본값).
    초과하면 리스크팩터를 낮춰서 매수금을 상한에 맞춘다.
    종목마다 리스크팩터가 달라지며, held_stock_record.json에 저장된다.

    예외 진입 (1주 가격이 총자본의 2% 초과):
      → 1주 매수, max_unit=2, effective_risk_factor=None

    Returns:
        (수량, 진입유형, 최대Unit, 유효리스크팩터) 튜플 또는 None(스킵)
        예: (200, "NORMAL", 3, 0.004) 또는 (1, "EXCEPTION", 2, None) 또는 None
    """
    name = get_watchlist().get(code, {}).get("name", code)

    # 1주 가격이 총자본에서 차지하는 비율
    one_share_ratio = price / total_capital if total_capital > 0 else 1.0

    # 1주 가격이 총자본 2% 초과 → 예외 진입 (1주, 최대 2 Unit)
    if one_share_ratio > 0.02:
        allow_exception = (
            os.getenv("LS_ALLOW_EXCEPTION_ENTRY", "True").strip().lower() == "true"
        )
        if not allow_exception:
            print(f"[turtle] {name}({code}) 예외 진입 비활성화(LS_ALLOW_EXCEPTION_ENTRY=False) → 스킵")
            return None
        print(f"[turtle] {name}({code}) 예외 진입 — 1주 가격({price:,}원)이 "
              f"총자본 {one_share_ratio*100:.1f}% → 1주 매수 (max_unit=2)")
        return (1, "EXCEPTION", 2, None)

    # 일반 진입: ATR 기반 수량 계산
    if atr_n <= 0:
        print(f"[turtle] {name}({code}) ATR(N)=0 → 수량 계산 불가, 스킵")
        return None

    # 기본 리스크팩터 0.02로 수량 계산
    # 공식: qty = (총자본 × 리스크팩터) / ATR
    qty = int((total_capital * 0.02) / atr_n)
    if qty <= 0:
        print(f"[turtle] {name}({code}) 계산된 수량=0 (ATR={atr_n:,.0f}) → 스킵")
        return None

    # 1 Unit 최대 매수금액 상한 확인 (총자본 × 10%)
    max_unit_amount = total_capital * MAX_UNIT_PURCHASE_RATIO
    purchase_amount = qty * price

    if purchase_amount <= max_unit_amount:
        # 상한 이내 → 리스크팩터 그대로 0.02
        effective_risk_factor = 0.02
    else:
        # 상한 초과 → 리스크팩터를 줄여서 매수금을 상한에 맞춤
        # 공식: rf = (상한금액 × ATR) / (총자본 × 현재가)
        effective_risk_factor = (max_unit_amount * atr_n) / (total_capital * price)
        qty = int((total_capital * effective_risk_factor) / atr_n)
        print(f"[turtle] {name}({code}) 매수금 상한 초과 → "
              f"리스크팩터 0.02 → {effective_risk_factor:.4f}로 조정 "
              f"({qty}주, 매수금: {qty * price:,.0f}원 / 상한: {max_unit_amount:,.0f}원)")

    return (qty, "NORMAL", 3, effective_risk_factor)
```

---

**수정 3 — place_entry_order() 시그니처 + 저장 필드 (241~310행)**
```python
# 시그니처 변경 전
def place_entry_order(
    code: str, qty: int, price: int, atr_n: float,
    entry_type: str, max_unit: int,
    entry_source: str = "TURTLE_S1"
):

# 시그니처 변경 후
def place_entry_order(
    code: str, qty: int, price: int, atr_n: float,
    entry_type: str, max_unit: int,
    entry_source: str = "TURTLE_S1",
    effective_risk_factor=None
):

# position_state 저장 — effective_risk_factor 필드 추가
position_state[code] = {
    "stock_name":            name,
    "current_unit":          1,
    "last_buy_price":        price,
    "avg_buy_price":         price,
    "stop_loss_price":       stop_loss_price,
    "next_pyramid_price":    next_pyramid_price,
    "entry_type":            entry_type,
    "max_unit":              max_unit,
    "total_qty":             qty,
    "entry_source":          entry_source,
    "effective_risk_factor": effective_risk_factor,   # ← 추가
}
```

---

**수정 4 — run_orders() 두 곳**

4-A. 신규 진입 언패킹 (538행):
```python
# 변경 전
qty, entry_type, max_unit = result
# (이후 place_entry_order 호출)
place_entry_order(code, qty, current_price, atr_n, entry_type, max_unit, entry_source)

# 변경 후
qty, entry_type, max_unit, effective_rf = result
# (이후 place_entry_order 호출)
place_entry_order(code, qty, current_price, atr_n, entry_type, max_unit, entry_source, effective_rf)
```

4-B. 피라미딩 qty 계산 (597~602행):
```python
# 변경 전
# 피라미딩용 Unit 수량 계산 (신규 진입과 동일한 방식)
result = calc_unit_size(code, current_price, atr_n, total_capital)
if result is None:
    continue
qty = result[0]  # qty만 사용 (entry_type, max_unit은 기존 pos에서 관리)

# 변경 후
# 피라미딩 수량: 진입 시 저장한 리스크팩터 재사용 (종목별 고정값)
effective_rf = pos.get("effective_risk_factor")
if effective_rf is None:
    # EXCEPTION / MANUAL 진입 종목 → 1주 고정
    qty = 1
else:
    qty = int((total_capital * effective_rf) / atr_n)
    if qty <= 0:
        continue
```

---

#### [balance_sync.py] 수정 1곳

**수정 1 — 수동 편입 dict 필드 추가 (158행)**
```python
# 변경 전
held[code] = {
    "stock_name":         name,
    "current_unit":       1,
    "last_buy_price":     avg_price,
    "avg_buy_price":      avg_price,
    "stop_loss_price":    stop_loss_price,
    "next_pyramid_price": 0,
    "entry_type":         "MANUAL",
    "max_unit":           1,
    "total_qty":          qty,
    "source":             "MANUAL_SYNC",
}

# 변경 후
held[code] = {
    "stock_name":            name,
    "current_unit":          1,
    "last_buy_price":        avg_price,
    "avg_buy_price":         avg_price,
    "stop_loss_price":       stop_loss_price,
    "next_pyramid_price":    0,
    "entry_type":            "MANUAL",
    "max_unit":              1,
    "total_qty":             qty,
    "source":                "MANUAL_SYNC",
    "effective_risk_factor": None,   # ← 추가
}
```

---

## PHASE 3 — 구현

### P3-1 Phase별 구현 계획 수립 ✅

#### 구현 Phase 목록

| 구현 Phase | 파일 | 작업 내용 |
|-----------|------|---------|
| Phase A | `turtle_order_logic.py` | 상수 추가 + calc_unit_size 교체 + place_entry_order 수정 + run_orders 수정 |
| Phase B | `balance_sync.py` | 수동 편입 dict 필드 1줄 추가 |

**순서 근거:** Phase A가 핵심 로직, Phase B는 독립적 소수정 → A 먼저 완료 후 B

---

## PHASE 4 — 테스트

### P4-1 단위 테스트 ✅
#### 테스트 결과

테스트 파일: `test_p4_unit_risk_factor.py` (9개)

| # | 테스트 케이스 | 결과 |
|---|-------------|------|
| 1 | 매수금 상한 이내 → effective_risk_factor = 0.02 유지 | ✅ |
| 2 | 매수금 상한 초과 → effective_risk_factor 감소, 매수금 상한에 맞춤 | ✅ |
| 3 | 상한 초과 후 매수금이 반드시 상한 이하임을 확인 | ✅ |
| 4 | 예외 진입 (1주 가격 > 총자본 2%) → (1, EXCEPTION, 2, None) | ✅ |
| 5 | 예외 진입 비활성화(LS_ALLOW_EXCEPTION_ENTRY=False) → None | ✅ |
| 6 | ATR = 0 → None 반환 | ✅ |
| 7 | qty = 0 (ATR 극단값) → None 반환 | ✅ |
| 8 | 반환값 4개 항목 구조 확인 | ✅ |
| 9 | 일반 진입 max_unit = 3 확인 | ✅ |

부수 수정: 예외 진입 출력문의 em dash(—) → 콜론(:) 변경 (Windows cp949 인코딩 호환)

### P4-2 통합 테스트 ✅
#### 테스트 결과

테스트 파일: `test_p4_2_risk_factor_pipeline.py` (6개)

| # | 테스트 케이스 | 결과 |
|---|-------------|------|
| 1 | 진입 후 held_stock_record에 effective_risk_factor 필드 저장 확인 | ✅ |
| 2 | 상한 이내 진입 → 0.02 저장 | ✅ |
| 3 | 상한 초과 진입 → 조정된 리스크팩터(0.002) 저장 | ✅ |
| 4 | 피라미딩 시 저장된 effective_risk_factor로 수량 계산 | ✅ |
| 5 | EXCEPTION 진입 후 피라미딩 → qty=1 고정 | ✅ |
| 6 | 2N 손절가와 effective_risk_factor가 함께 저장됨 | ✅ |

### P4-3 무한루프·오류 안전성 확인 ✅
#### 확인 결과

테스트 파일: `test_p4_3_risk_factor_safety.py` (6개)

| # | 확인 항목 | 결과 |
|---|---------|------|
| 1 | 구버전 NORMAL 종목 (필드 없음) → 기본값 0.02 사용 | ✅ |
| 2 | 구버전 EXCEPTION 종목 (필드 없음) → qty=1 고정 | ✅ |
| 3 | effective_risk_factor=0.0 → 피라미딩 스킵 | ✅ |
| 4 | total_capital=0 → ZeroDivisionError 없음 | ✅ |
| 5 | price=0 → ZeroDivisionError 없음 | ✅ |
| 6 | effective_risk_factor 음수 → 피라미딩 스킵 | ✅ |

**추가 수정**: 피라미딩 로직에 구버전 호환 처리 추가
- `effective_risk_factor` 필드 없는 NORMAL 종목 → 0.02 기본값 사용 (오배포 방지)

---

## PHASE 5 — 마무리

### P5-1 최종 사용자 승인 ⬜
### P5-2 관련 문서 업데이트 ⬜

---

## 📝 메모 및 특이사항

(작업 중 기억해야 할 것들을 여기에 기록)

---

## ✅ 최종 완료 체크리스트

- [ ] 모든 Phase 완료 (P0 ~ P5)
- [ ] 단위 테스트 통과
- [ ] 통합 테스트 통과
- [ ] 무한루프 안전성 확인
- [ ] 사용자 최종 승인 완료
- [ ] 관련 문서 업데이트 완료
